import { getUserData } from '@/lib/data'
import { formatMoney } from '@/lib/utils'
export default async function Reports(){const {buckets,transactions}=await getUserData(); const expense=transactions.filter((t:any)=>t.type==='expense'); const total=expense.reduce((s:any,t:any)=>s+Number(t.amount),0); return <div><h1 className="text-3xl font-black">Báo cáo</h1><section className="mt-6 grid gap-4 md:grid-cols-3"><div className="card p-5"><p className="text-sm text-slate-500">Tổng đã chi</p><b className="text-2xl">{formatMoney(total)}</b></div><div className="card p-5"><p className="text-sm text-slate-500">Số hũ</p><b className="text-2xl">{buckets.length}</b></div><div className="card p-5"><p className="text-sm text-slate-500">Giao dịch</p><b className="text-2xl">{transactions.length}</b></div></section><div className="card mt-6 p-6"><h2 className="font-black">Tỷ trọng hũ tài chính</h2><div className="mt-5 space-y-4">{buckets.map((b:any)=>{const max=Math.max(...buckets.map((x:any)=>Number(x.current_balance)),1);return <div key={b.id}><div className="flex justify-between text-sm"><b>{b.name}</b><span>{formatMoney(b.current_balance)}</span></div><div className="mt-2 h-4 rounded-full bg-slate-100"><div className="h-4 rounded-full" style={{width:`${Number(b.current_balance)/max*100}%`,background:b.color}}/></div></div>})}</div></div></div>}
